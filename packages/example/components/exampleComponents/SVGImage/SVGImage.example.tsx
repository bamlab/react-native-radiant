import { View } from 'react-native';
import SVGTestImage from '@components/exampleComponents/SVGImage/default-svg';

const SVGImage = () => {
  return (
    <View>
      <SVGTestImage width={100} height={100} />
    </View>
  );
};

export default SVGImage;
